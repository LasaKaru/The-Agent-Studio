"use client";
import { useState } from "react";
import { StudioAgentState } from "@/app/types/studio";

export function AgentModal({ agent, onClose, onFeedback }: { agent?: StudioAgentState; onClose: () => void; onFeedback: (message: string) => void }) {
  const [feedback, setFeedback] = useState(""); if (!agent) return null;
  return <div className="fixed inset-0 z-50 bg-black/70 p-6" onClick={onClose}><div onClick={(e)=>e.stopPropagation()} className="glass mx-auto mt-10 max-h-[86vh] w-[min(760px,92vw)] overflow-auto rounded-3xl p-6"><h2 className="text-2xl font-semibold">{agent.icon} {agent.name}</h2><p className="mt-1 text-cyan-200">{agent.role}</p><div className="mt-6 grid gap-4 md:grid-cols-2"><section><h4 className="font-semibold">Visible reasoning</h4><pre className="mt-2 whitespace-pre-wrap rounded-2xl bg-slate-950 p-4 text-sm text-slate-300">{agent.output?.reasoning || "No reasoning captured yet."}</pre></section><section><h4 className="font-semibold">Output</h4><pre className="mt-2 whitespace-pre-wrap rounded-2xl bg-slate-950 p-4 text-sm text-slate-300">{agent.output?.output || "Run the pipeline to create output."}</pre></section></div><label className="mt-5 block text-sm text-slate-300">Human feedback</label><textarea value={feedback} onChange={(e) => setFeedback(e.target.value)} className="mt-2 min-h-28 w-full rounded-2xl border border-slate-700 bg-slate-950 p-4 outline-none focus:border-cyan-300" placeholder="Ask this agent to adjust its work..."/><div className="mt-4 flex justify-end gap-3"><button onClick={onClose} className="rounded-full border border-slate-700 px-4 py-2">Close</button><button disabled={!feedback.trim()} onClick={() => { onFeedback(feedback); setFeedback(""); }} className="rounded-full bg-cyan-300 px-4 py-2 font-semibold text-slate-950 disabled:opacity-50">Re-run with feedback</button></div></div></div>;
}
